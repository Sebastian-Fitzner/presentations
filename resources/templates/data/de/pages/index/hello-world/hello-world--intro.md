Legen wir einfach mal eine neue Datei `hello.js` an. Darin platzieren wir folgenden Code:

``` js
console.log('Hello World');
```

Nun rufen wir die Datei einfach über die Konsole per `node hello` auf!